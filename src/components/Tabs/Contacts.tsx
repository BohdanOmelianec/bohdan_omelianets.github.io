
import styled from 'styled-components';

const TabWrapper = styled.div`
  width: 100%;
  height: 100%;
  display: grid;
  place-items: center;
`

const Contacts = () => {
  return (
    <TabWrapper>
      Contacts
    </TabWrapper>
  )
};

export default Contacts;